<% provide(:title, @user.name) %>
<div class="row">
	<aside class="span=4">
		<section>
			<h1>
				<%= gravatar_for @user %>
				<%= @user.name %>
			</h1>
		</section>
	</aside>
</div>

<%= @user.name %>, <%= @user.email %>
